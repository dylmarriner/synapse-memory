[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "synapse-sdk"
version = "0.1.0"
description = "Python SDK for Synapse Enterprise Memory"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
