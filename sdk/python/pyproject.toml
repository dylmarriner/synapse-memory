[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nexus-sdk"
version = "1.0.0"
description = "Python client for the Nexus unified AI memory server"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/dylanmarriner/shared-memory-mcp"
