#!/usr/bin/env python3
"""Run a command through Obelisk and report compact telemetry to Nexus.

Usage:
  nexus-obelisk git status
  nexus-obelisk pytest
  NEXUS_OBELISK_DURABLE=1 NEXUS_OBELISK_SUMMARY="pytest failed because migrations are missing" nexus-obelisk pytest

This wrapper intentionally does not send raw command output to Nexus by default.
It prints Obelisk-filtered output to stdout/stderr for the agent and posts
metadata to the Obelisk event endpoint.
"""

from __future__ import annotations

import json
import os
import shutil
import subprocess
import sys
import time
import urllib.request
import urllib.error


def _estimate_tokens(chars: int) -> int:
    return max(0, chars // 4)


def _post_event(payload: dict) -> None:
    url = os.getenv("NEXUS_URL", "http://localhost:7777").rstrip("/") + "/v1/obelisk/events"
    secret = os.getenv("NEXUS_SECRET", "")
    if not secret:
        return
    req = urllib.request.Request(
        url,
        data=json.dumps(payload).encode(),
        headers={"Content-Type": "application/json", "Authorization": f"Bearer {secret}"},
        method="POST",
    )
    try:
        urllib.request.urlopen(req, timeout=3).read()
    except Exception:
        # Never break the user's command because telemetry failed.
        pass


def main() -> int:
    if len(sys.argv) < 2:
        print("Usage: nexus-obelisk <command> [args...]", file=sys.stderr)
        return 2
    if shutil.which("obelisk") is None:
        print("nexus-obelisk: obelisk not found on PATH", file=sys.stderr)
        return 127

    cmd = sys.argv[1:]
    started = time.time()
    proc = subprocess.run(["obelisk", "run", *cmd], text=True, capture_output=True)
    duration_ms = int((time.time() - started) * 1000)

    if proc.stdout:
        print(proc.stdout, end="")
    if proc.stderr:
        print(proc.stderr, end="", file=sys.stderr)

    filtered_chars = len(proc.stdout) + len(proc.stderr)
    tokens_saved = int(os.getenv("NEXUS_OBELISK_TOKENS_SAVED", "0") or "0")
    summary = os.getenv("NEXUS_OBELISK_SUMMARY", "").strip() or None
    durable = os.getenv("NEXUS_OBELISK_DURABLE", "0").lower() in {"1", "true", "yes"}
    if durable and summary is None and proc.returncode != 0:
        summary = f"Obelisk-wrapped command failed: {' '.join(cmd[:3])} exited {proc.returncode}."

    _post_event({
        "agent_id": os.getenv("NEXUS_AGENT_ID", "obelisk-agent"),
        "command": " ".join(cmd),
        "exit_code": proc.returncode,
        "duration_ms": duration_ms,
        "cwd": os.getcwd(),
        "output_chars": 0,
        "filtered_chars": filtered_chars,
        "tokens_saved_estimate": tokens_saved or _estimate_tokens(0),
        "summary": summary,
        "durable": durable,
        "importance": float(os.getenv("NEXUS_OBELISK_IMPORTANCE", "0.55")),
        "tags": ["obelisk", "command"],
        "metadata": {"wrapper": "nexus-obelisk"},
    })
    return proc.returncode


if __name__ == "__main__":
    raise SystemExit(main())
