# OpenClaw Obelisk Instructions

Run OpenClaw with `obelisk` on PATH. Keep the Nexus Memory plugin for durable memory.
Use `scripts/nexus-obelisk` for noisy shell commands when available, otherwise
`obelisk run`. Use raw commands when exact output is required.
