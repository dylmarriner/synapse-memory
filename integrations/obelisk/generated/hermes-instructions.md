# Hermes Obelisk Instructions

Run Hermes with `obelisk` on PATH. Use Nexus plugin for memory. Prefix noisy shell
commands with `scripts/nexus-obelisk` when available, otherwise `obelisk run`;
use raw commands when full unfiltered output is required.
