fastapi>=0.109
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
redis[hiredis]>=5.0
httpx>=0.27
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.9
pgvector>=0.3
openai>=1.30
click>=8.1  # `nexus` CLI entrypoint (embedded install mode)

# Embedded mode (EMBEDDED_MODE=true): in-process worker + fakeredis, no Redis service
fakeredis>=2.20
aiosqlite>=0.20

fastembed>=0.5.0

# Code-context: tree-sitter for symbol extraction (Python .py uses stdlib ast;
# JS/TS/Go/Rust/TSX use tree-sitter-languages for AST parsing).
tree-sitter>=0.21
tree-sitter-languages>=1.10
inotify_simple>=2.0  # real-time file-change watcher for the indexer
aiohttp>=3.9  # async HTTP client used by the code watcher

# Optional: local cross-encoder reranker (RERANKER_PROVIDER=local)
# sentence-transformers>=3.0

# Optional: OpenTelemetry observability (OTEL_ENABLED=true)
# opentelemetry-sdk>=1.25
# opentelemetry-api>=1.25
# opentelemetry-instrumentation-fastapi>=0.46b0
# opentelemetry-instrumentation-sqlalchemy>=0.46b0
# opentelemetry-instrumentation-redis>=0.46b0
